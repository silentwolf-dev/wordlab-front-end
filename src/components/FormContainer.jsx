import React from 'react'

const formContainer = ({width, height}) => {
    return (
        <div className="form-container" style={{width: width, height: height,}}>
            
        </div>
    )
}

export default formContainer;
